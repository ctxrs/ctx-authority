# Fixture values are synthetic and must never be real provider credentials.
PLAIN=abc
SPACED=value with spaces   # trailing comment
QUOTED_HASH="value#not-comment"
SINGLE_QUOTED='literal \n value'
DOUBLE_QUOTED="line\nnext\tend"
ESCAPED_HASH=value\#still-value
EMPTY=
export EXPORTED=from-export
